model Post {
  id        Int      @id @default(autoincrement())
  title     String
  content   String
  published Boolean  @default(false)
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  email2     String   @unique
}